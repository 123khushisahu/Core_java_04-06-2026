package com.core.Collection.List;

public class Vector_example{

	public static void main(String[] args) {
		/*Vector in Java
Definition (English)

A Vector is a legacy class in Java that implements the List interface. It stores elements in insertion order, allows duplicate elements, and is synchronized (thread-safe).

Definition (Hindi)

Vector Java Collections Framework ki ek legacy class hai jo List interface ko implement karti hai. Ye insertion order maintain karti hai, duplicate values allow karti hai aur synchronized (thread-safe) hoti hai.

Features of Vector
✅ Maintains insertion order
✅ Allows duplicate elements
✅ Allows null values
✅ Automatically grows when full
✅ Thread-safe (synchronized)
❌ Slower than ArrayList because synchronization adds overhead*/
		

	}

}
